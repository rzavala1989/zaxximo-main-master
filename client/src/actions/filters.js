import { SORT_BY_COMPLETE, SORT_BY_DATE } from './types';

// SORT_BY_DATE
export const sortByDate = () => ({
  type: SORT_BY_DATE,
});

// SORT_BY_AMOUNT
export const sortByComplete = () => ({
  type: SORT_BY_COMPLETE,
});
